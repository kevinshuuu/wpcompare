.container
  .left-column
    = render partial: 'user_data'

  .center-column
    .query-container
      = render partial: 'form'

    = render partial: 'charts'

  .right-column
    = render partial: 'user_data'
